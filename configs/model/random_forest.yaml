# @package model
type: "RandomForestRegressor"
n_estimators: 100
max_depth: 10
min_samples_split: 2
min_samples_leaf: 1
max_features: "sqrt"
bootstrap: true
random_state: ${seed}
n_jobs: -1

_validate:
  n_estimators:
    min: 10
    max: 1000
  max_depth:
    min: 1
    max: 50