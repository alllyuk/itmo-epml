# @package model
type: "GradientBoostingRegressor"
n_estimators: 100
learning_rate: 0.1
max_depth: 5
min_samples_split: 2
min_samples_leaf: 1
subsample: 0.8
random_state: ${seed}

_validate:
  learning_rate:
    min: 0.001
    max: 1.0
  n_estimators:
    min: 10
    max: 1000